import renderer from './renderer';
import {clear} from 'game/resources';
import preload from './preload';

export default () => {
  clear();
  const tasks = [renderer(), preload()];
  return Promise.all(tasks);
};
